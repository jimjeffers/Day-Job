<%= link_to '&lt; Return to Features', project_features_path(@project), :id => "step_back" %>

<h2>New feature</h2>

<%= render :partial => 'project_form', :locals => {:project => @project, :feature => @feature, :submit => "Create Feature"} %>